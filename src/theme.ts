const color = {
  primaryColor: '#14746F',
  lightColor: '#78C6A3',
  backgroundColor: '#DEF7EC',
  borderColor: '#C1C1C1',
};

export type Color = typeof color;

const theme = {
  color,
};

export default theme;
